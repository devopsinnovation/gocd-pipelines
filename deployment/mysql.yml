format_version: 10
pipelines:
  MYSQL:
    group: APP-Deployments
    label_template: ${COUNT}
    lock_behavior: none
    display_order: -1
    materials:
      git-68ec9f0:
        git: https://github.com/devopsinnovation/ansible-expense-dry-code
        shallow_clone: false
        auto_update: true
        branch: main
        secure_variables:
          SSH_PASSWORD: "AES:ynUJLO5CSsjpYYX1M7je5w==:gHzjovJvFVP2sIpRhIKwMw=="
        environment_variables:
          component: mysql
    stages:
      - DEV:
          fetch_materials: true
          environment_variables:
            env: dev
            component: mysql
          clean_workspace: false
          jobs:
            ansible:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                    - run.sh
                    command: bash

      - QA:
          fetch_materials: true
          clean_workspace: false
          approval:
            type: manual
          jobs:
            ansible:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                      - -l
                    command: ls
                    run_if: passed

      - STAGE:
          fetch_materials: true
          clean_workspace: false
          jobs:
            ansible:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                      - -l
                    command: ls
                    run_if: passed
      - PROD:
          fetch_materials: true
          clean_workspace: false
          jobs:
            ansible:
              timeout: 0
              tasks:
                - exec:
                    arguments:
                      - -l
                    command: ls
                    run_if: passed